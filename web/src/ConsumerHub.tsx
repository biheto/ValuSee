import { ArrowLeft, Bell, Bookmark, ChevronRight, Clock3, Crown, ExternalLink, FolderPlus, Heart, History, Laptop, MessageSquare, PackageCheck, Receipt, RotateCcw, Search, ShieldCheck, Sparkles, Tag, Trash2, UserRound, WalletCards, X } from 'lucide-react';
import { useEffect, useState } from 'react';

export type ConsumerProduct = {
  title: string; platform: string; url: string; brand: string; model: string; sku: string;
  specs: Record<string, string>; price: number; coupon: number; platform_discount: number;
  member_discount: number; subsidy: number; pay_discount: number; shipping: number; gift_value: number;
  condition: string; official_store: boolean; return_days: number; warranty_months: number; notes: string;
};
export type SavedItem = { saved_id: string; item_type: 'favorite' | 'recent' | 'brand'; reference_key: string; label: string; product: ConsumerProduct; group_id?: string | null; group_name?: string | null; updated_at: string };
export type SavedGroup = { group_id: string; name: string; item_count: number };
export type Dashboard = { reports?: number; monitors?: number; purchases?: number; unread?: number; favorite?: number; recent?: number; brand?: number; actual_savings?: number };
export type ConsumerNotification = { notification_id: string; kind?: string; title: string; message: string; status: string; target_url?: string; delivery?: { status: string; result: string; attempt: number } | null; created_at: string };
export type ProductDetailData = {
  product_ref: string; product: ConsumerProduct; offers: ConsumerProduct[]; alternatives: ConsumerProduct[]; updated_at: string;
  price_history: { snapshots?: Array<{ captured_at: string; final_price: number }>; lowest_price?: number; average_price?: number };
  review_evidence?: { report: { risk_level: string; confidence: number; summary: string; sample_size: number; issue_groups: Array<{ name: string; ratio: number; evidence: string[] }> }; sources: string[]; created_at: string } | null;
};
export type ContentItem = { content_id: string; content_type: string; title: string; summary: string; body: string; category: string; source_url?: string; published_at?: string };
type CampaignItem = { campaign_id: string; title: string; summary: string; placement: string; target_url?: string };

const money = (value?: number) => `¥${Number(value || 0).toFixed(0)}`;
const day = (value: string) => new Date(value).toLocaleDateString('zh-CN', { month: 'short', day: 'numeric' });

export function DiscoverPage({ dashboard, recent, onStart, onOpen, onOpenContent }: { dashboard: Dashboard; recent: SavedItem[]; onStart: (goal: string) => void; onOpen: (product: ConsumerProduct) => void; onOpenContent: (contentId: string) => void }) {
  const [query, setQuery] = useState('');
  const [content, setContent] = useState<ContentItem[]>([]);
  const [campaigns, setCampaigns] = useState<CampaignItem[]>([]);
  const [contentQuery, setContentQuery] = useState('');
  const [contentCategory, setContentCategory] = useState('');
  const [contentCategories, setContentCategories] = useState<string[]>([]);
  useEffect(() => { void fetch('/api/v1/shopping/content').then((response) => response.json()).then((data) => { setContent(data.items || []); setContentCategories(data.categories || []); }).catch(() => setContent([])); }, []);
  useEffect(() => { void fetch('/api/v1/shopping/campaigns').then((response) => response.json()).then((data) => setCampaigns(data.items || [])).catch(() => setCampaigns([])); }, []);
  const categories = [['手机', '换机前先核对版本与补贴'], ['电脑', '性能、续航和接口一起看'], ['显示器', '尺寸、供电与色彩不踩坑'], ['耳机', '降噪、接口与售后都算清'], ['家电', '能耗、耗材与保修全周期']];
  return <section className="discover-page">
    <div className="discover-hero"><div><span><Sparkles size={16} />AI 购物决策</span><h1>买之前，先看清真正的价值。</h1><p>把需求、商品链接或截图交给 ValuSee，从同款识别、到手价到买后保价，一次算明白。</p><form onSubmit={(event) => { event.preventDefault(); if (query.trim()) onStart(query.trim()); }}><Search size={20} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="想买什么？例如：适合 MacBook 的 27 英寸显示器" /><button>开始分析</button></form></div><div className="discover-value"><strong>{money(dashboard.actual_savings)}</strong><span>已记录节省</span><small>{dashboard.monitors || 0} 个监控正在守候目标价</small></div></div>
    <div className="category-strip">{categories.map(([name, description]) => <button key={name} onClick={() => onStart(`帮我选${name}，请结合预算、用途和已有设备推荐`)}><i><Tag size={18} /></i><strong>{name}</strong><span>{description}</span><ChevronRight size={16} /></button>)}</div>
    {campaigns.length > 0 && <section className="campaign-strip" aria-label="当前活动">{campaigns.slice(0, 3).map((item) => <article key={item.campaign_id}><span>ValuSee 活动 · {item.placement}</span><strong>{item.title}</strong><p>{item.summary}</p>{item.target_url && <a href={item.target_url} target="_blank" rel="noreferrer">查看详情<ExternalLink size={13} /></a>}</article>)}</section>}
    {content.length > 0 && <section className="content-shelf"><div className="consumer-section-title"><div><span>选购指南</span><h2>把复杂参数讲清楚</h2></div><BookOpenIcon /></div><div className="content-filter"><label><Search size={14} /><input value={contentQuery} onChange={(event) => setContentQuery(event.target.value)} placeholder="搜索指南与专题" /></label><select value={contentCategory} onChange={(event) => setContentCategory(event.target.value)}><option value="">全部分类</option>{contentCategories.map((category) => <option key={category}>{category}</option>)}</select></div><div className="content-grid">{content.filter((item) => (!contentCategory || item.category === contentCategory) && (!contentQuery.trim() || `${item.title} ${item.summary} ${item.body}`.toLowerCase().includes(contentQuery.trim().toLowerCase()))).map((item) => <article key={item.content_id}><span>{item.category} · {item.content_type}</span><h3>{item.title}</h3><p>{item.summary}</p><div><button onClick={() => onOpenContent(item.content_id)}>阅读全文<ChevronRight size={13} /></button>{item.source_url && <a href={item.source_url} target="_blank" rel="noreferrer"><ExternalLink size={13} />来源</a>}</div></article>)}</div></section>}
    <div className="discover-grid"><section><div className="consumer-section-title"><div><span>继续上次</span><h2>最近看过</h2></div><History size={20} /></div>{recent.length ? <div className="consumer-product-grid">{recent.slice(0, 6).map((item) => <ProductTile key={item.saved_id} product={item.product} subtitle={`${item.product.platform || '来源待确认'} · ${day(item.updated_at)}`} onOpen={() => onOpen(item.product)} />)}</div> : <EmptyState icon={<Clock3 />} title="还没有浏览足迹" text="搜索或打开商品后，会在这里继续你的购买决策。" />}</section><aside><div className="consumer-section-title"><div><span>你的进度</span><h2>购物助理摘要</h2></div><WalletCards size={20} /></div><div className="home-stats"><article><Bell /><strong>{dashboard.monitors || 0}</strong><span>降价监控</span></article><article><Heart /><strong>{dashboard.favorite || 0}</strong><span>收藏商品</span></article><article><Receipt /><strong>{dashboard.purchases || 0}</strong><span>购买记录</span></article><article><MessageSquare /><strong>{dashboard.unread || 0}</strong><span>未读提醒</span></article></div><div className="truth-note"><ShieldCheck size={20} /><div><strong>真实来源优先</strong><p>没有授权价格或可追溯证据时，ValuSee 会明确提示缺失，不生成虚假榜单和优惠。</p></div></div></aside></div>
  </section>;
}

export function ContentDetailPage({ contentId, onBack, onOpenContent }: { contentId: string; onBack: () => void; onOpenContent: (contentId: string) => void }) {
  const [data, setData] = useState<{ item: ContentItem; related: ContentItem[] } | null>(null);
  const [error, setError] = useState('');
  useEffect(() => { setData(null); setError(''); void fetch(`/api/v1/shopping/content/${encodeURIComponent(contentId)}`).then(async (response) => { if (!response.ok) throw new Error('内容不存在或已下线'); return response.json(); }).then(setData).catch((reason) => setError(reason instanceof Error ? reason.message : '内容读取失败')); }, [contentId]);
  if (!data) return <main className="content-detail-page"><button className="content-back" onClick={onBack}><ArrowLeft size={16} />返回发现</button><div className="content-loading" role={error ? 'alert' : 'status'}>{error || '正在读取内容...'}</div></main>;
  const { item, related } = data;
  return <main className="content-detail-page"><button className="content-back" onClick={onBack}><ArrowLeft size={16} />返回发现</button><article><header><span>{item.category} · {item.content_type}</span><h1>{item.title}</h1><p>{item.summary}</p><small>{item.published_at ? day(item.published_at) : '发布时间待确认'}</small></header><div className="content-body">{item.body.split(/\n{2,}/).filter(Boolean).map((paragraph, index) => <p key={index}>{paragraph}</p>)}</div>{item.source_url && <a className="content-source" href={item.source_url} target="_blank" rel="noreferrer"><ExternalLink size={15} />查看原始资料来源</a>}</article>{related.length > 0 && <aside><h2>相关推荐</h2><div>{related.map((content) => <button key={content.content_id} onClick={() => onOpenContent(content.content_id)}><span>{content.category}</span><strong>{content.title}</strong><ChevronRight size={15} /></button>)}</div></aside>}</main>;
}

function BookOpenIcon() { return <Bookmark size={20} />; }

function ProductTile({ product, subtitle, onOpen }: { product: ConsumerProduct; subtitle: string; onOpen: () => void }) {
  return <button className="consumer-product-tile" onClick={onOpen}><div className="product-placeholder"><Laptop size={28} /></div><div><strong>{product.title || '未命名商品'}</strong><span>{subtitle}</span><b>{product.price ? money(product.price) : '价格待确认'}</b></div><ChevronRight size={17} /></button>;
}

export function SavedPage({ items, groups, onOpen, onDelete, onCreateGroup, onBulk }: { items: SavedItem[]; groups: SavedGroup[]; onOpen: (product: ConsumerProduct) => void; onDelete: (id: string) => void; onCreateGroup: (name: string) => void; onBulk: (ids: string[], action: 'delete' | 'move', groupId?: string) => void }) {
  const [tab, setTab] = useState<'favorite' | 'recent' | 'brand'>('favorite');
  const [query, setQuery] = useState('');
  const [groupId, setGroupId] = useState('');
  const [selected, setSelected] = useState<string[]>([]);
  const [newGroup, setNewGroup] = useState('');
  const tabs: Array<[typeof tab, string]> = [['favorite', '收藏'], ['recent', '足迹'], ['brand', '关注品牌']];
  const rows = items.filter((item) => item.item_type === tab && (!query.trim() || `${item.label} ${item.product?.brand || ''} ${item.product?.model || ''}`.toLowerCase().includes(query.trim().toLowerCase())) && (!groupId || item.group_id === groupId));
  function toggle(id: string) { setSelected((ids) => ids.includes(id) ? ids.filter((item) => item !== id) : [...ids, id]); }
  return <section className="page-section"><div className="consumer-page-head"><div><span>我的关注</span><h1>收藏与足迹</h1><p>把有兴趣的商品、品牌和最近浏览留在账户里。</p></div><Bookmark size={28} /></div><div className="consumer-tabs">{tabs.map(([key, label]) => <button className={tab === key ? 'active' : ''} onClick={() => { setTab(key); setSelected([]); }} key={key}>{label}<b>{items.filter((item) => item.item_type === key).length}</b></button>)}</div><div className="saved-toolbar panel"><label><Search size={15} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="搜索收藏、品牌或型号" /></label><select value={groupId} onChange={(event) => setGroupId(event.target.value)}><option value="">全部分组</option>{groups.map((group) => <option key={group.group_id} value={group.group_id}>{group.name} ({group.item_count})</option>)}</select><form onSubmit={(event) => { event.preventDefault(); if (newGroup.trim()) { onCreateGroup(newGroup); setNewGroup(''); } }}><input value={newGroup} onChange={(event) => setNewGroup(event.target.value)} placeholder="新分组" /><button title="创建分组"><FolderPlus size={15} /></button></form></div>{selected.length > 0 && <div className="saved-bulk"><b>已选 {selected.length} 项</b><select defaultValue="" onChange={(event) => { if (event.target.value) { onBulk(selected, 'move', event.target.value); setSelected([]); event.target.value = ''; } }}><option value="">移动到分组</option>{groups.map((group) => <option key={group.group_id} value={group.group_id}>{group.name}</option>)}</select><button onClick={() => { onBulk(selected, 'delete'); setSelected([]); }}><Trash2 size={14} />批量删除</button></div>}<div className="panel saved-list">{rows.length ? rows.map((item) => <article key={item.saved_id}><input type="checkbox" aria-label={`选择 ${item.label}`} checked={selected.includes(item.saved_id)} onChange={() => toggle(item.saved_id)} /><button className="saved-main" onClick={() => item.item_type !== 'brand' && onOpen(item.product)}><div className="saved-icon">{item.item_type === 'brand' ? <Tag /> : <Laptop />}</div><div><strong>{item.label}</strong><span>{item.item_type === 'brand' ? '关注品牌' : `${item.product.platform || '来源待确认'} · ${day(item.updated_at)}`}{item.group_name ? ` · ${item.group_name}` : ''}</span></div>{item.item_type !== 'brand' && <b>{item.product.price ? money(item.product.price) : '价格待确认'}</b>}</button><button className="icon-button danger" title={item.item_type === 'brand' ? '取消关注' : '删除'} onClick={() => onDelete(item.saved_id)}><Trash2 size={15} /></button></article>) : <EmptyState icon={<Heart />} title="没有匹配内容" text="调整搜索、分组或在商品详情中添加收藏。" />}</div></section>;
}

export function MessagesPage({ notifications, onRead, onReadAll, onDelete, onRetry, onBulk, onNavigate }: { notifications: ConsumerNotification[]; onRead: (id: string) => void; onReadAll: () => void; onDelete: (id: string) => void; onRetry: (id: string) => void; onBulk: (ids: string[], action: 'read' | 'delete') => void; onNavigate: (target: string) => void }) {
  const [filter, setFilter] = useState<'all' | 'price' | 'after_sales' | 'system'>('all');
  const [unreadOnly, setUnreadOnly] = useState(false);
  const [selected, setSelected] = useState<string[]>([]);
  const classify = (kind = '') => ['price_reached', 'price_drop', 'monitor'].includes(kind) ? 'price' : ['price_protection', 'return_deadline', 'warranty_deadline', 'after_sales'].includes(kind) ? 'after_sales' : 'system';
  const rows = notifications.filter((item) => (filter === 'all' || classify(item.kind) === filter) && (!unreadOnly || item.status === 'unread'));
  function toggle(id: string) { setSelected((ids) => ids.includes(id) ? ids.filter((item) => item !== id) : [...ids, id]); }
  return <section className="page-section"><div className="consumer-page-head"><div><span>提醒中心</span><h1>消息</h1><p>降价、保价、退货与系统提醒集中处理。</p></div><Bell size={28} /></div><div className="message-toolbar"><div className="consumer-tabs">{([['all', '全部'], ['price', '价格'], ['after_sales', '售后'], ['system', '系统']] as const).map(([key, label]) => <button className={filter === key ? 'active' : ''} onClick={() => setFilter(key)} key={key}>{label}</button>)}</div><label className="message-unread"><input type="checkbox" checked={unreadOnly} onChange={(event) => setUnreadOnly(event.target.checked)} />只看未读</label><button className="soft-button" onClick={onReadAll}>全部已读</button></div>{selected.length > 0 && <div className="message-bulk"><b>已选 {selected.length} 条</b><button onClick={() => { onBulk(selected, 'read'); setSelected([]); }}>标为已读</button><button onClick={() => { onBulk(selected, 'delete'); setSelected([]); }}>删除</button></div>}<div className="panel message-center">{rows.length ? rows.map((item) => <article key={item.notification_id} className={item.status === 'unread' ? 'unread' : ''}><input type="checkbox" aria-label={`选择 ${item.title}`} checked={selected.includes(item.notification_id)} onChange={() => toggle(item.notification_id)} /><button className="message-main" onClick={() => { if (item.status === 'unread') onRead(item.notification_id); if (item.target_url) onNavigate(item.target_url); }}><i><Bell size={17} /></i><div><strong>{item.title}</strong><span>{item.message}</span><small>{day(item.created_at)} · {classify(item.kind) === 'price' ? '价格提醒' : classify(item.kind) === 'after_sales' ? '售后提醒' : '系统消息'}</small></div>{item.status === 'unread' && <em>未读</em>}</button><div className="message-actions">{item.delivery?.status === 'failed' && <button title="重发" onClick={() => onRetry(item.notification_id)}><RotateCcw size={14} /></button>}<button title="删除" onClick={() => onDelete(item.notification_id)}><Trash2 size={14} /></button></div></article>) : <EmptyState icon={<MessageSquare />} title="没有匹配消息" text="达到目标价或临近售后期限时，提醒会出现在这里。" />}</div></section>;
}

export function AccountHome({ name, dashboard, onNavigate, onLogin }: { name: string; dashboard: Dashboard; onNavigate: (view: string) => void; onLogin: () => void }) {
  const loggedIn = Boolean(localStorage.getItem('valuesee-token'));
  return <section className="page-section"><div className="profile-hero panel"><div className="profile-avatar"><UserRound size={30} /></div><div><span>{loggedIn ? 'ValuSee 用户' : '本地体验账户'}</span><h1>{name}</h1><p>{loggedIn ? '你的购物偏好、报告和提醒已跨设备同步。' : '登录后同步收藏、报告、监控和家庭数据。'}</p></div>{!loggedIn && <button className="primary-button" onClick={onLogin}>登录 / 注册</button>}</div><div className="profile-kpis"><article><strong>{money(dashboard.actual_savings)}</strong><span>累计记录节省</span></article><article><strong>{dashboard.reports || 0}</strong><span>决策报告</span></article><article><strong>{dashboard.favorite || 0}</strong><span>收藏商品</span></article><article><strong>{dashboard.purchases || 0}</strong><span>购买记录</span></article></div><div className="account-menu panel"><MenuRow icon={<Crown />} title="会员权益" text="查看当前额度与 Pro 开通状态" onClick={() => onNavigate('membership')} /><MenuRow icon={<UserRound />} title="购物偏好与设备档案" text="预算、品牌、已有设备和风险偏好" onClick={() => onNavigate('settings')} /><MenuRow icon={<History />} title="报告与对比清单" text="回看购买结论，继续未完成的比较" onClick={() => onNavigate('history')} /><MenuRow icon={<PackageCheck />} title="家庭账户" text="共享家庭设备和售后提醒" onClick={() => onNavigate('family')} /><MenuRow icon={<ShieldCheck />} title="账户与数据安全" text="登录设备、邮箱验证、导出和注销" onClick={() => loggedIn ? onNavigate('security') : onLogin()} /></div></section>;
}

function MenuRow({ icon, title, text, onClick }: { icon: React.ReactNode; title: string; text: string; onClick: () => void }) { return <button onClick={onClick}><i>{icon}</i><div><strong>{title}</strong><span>{text}</span></div><ChevronRight size={18} /></button>; }

export function ProductDetail({ productRef, fallbackProduct, favorite, onClose, onFavorite, onCompare, onProductLoaded, request }: { productRef?: string; fallbackProduct?: ConsumerProduct | null; favorite: boolean; onClose: () => void; onFavorite: (product: ConsumerProduct) => void; onCompare: (product: ConsumerProduct) => void; onProductLoaded: (product: ConsumerProduct) => void; request: <T>(path: string) => Promise<T> }) {
  const [detail, setDetail] = useState<ProductDetailData | null>(null);
  const [loadError, setLoadError] = useState('');
  useEffect(() => {
    if (!productRef) return;
    setLoadError('');
    void request<ProductDetailData>(`/api/v1/shopping/products/${encodeURIComponent(productRef)}`)
      .then((data) => { setDetail(data); onProductLoaded(data.product); })
      .catch((error) => setLoadError(error instanceof Error ? error.message : '商品详情读取失败'));
  }, [productRef]);
  const product = detail?.product || fallbackProduct;
  if (!product) return <div className="detail-backdrop"><aside className="product-detail product-detail-loading"><button className="detail-close" title="关闭" onClick={onClose}><X /></button>{loadError ? <p role="alert">{loadError}</p> : <p role="status">正在读取商品与来源证据...</p>}</aside></div>;
  const history = detail?.price_history || {};
  const snapshots = [...(history.snapshots || [])].reverse();
  const values = snapshots.map((item) => Number(item.final_price));
  const min = Math.min(...values, 0), max = Math.max(...values, 1), range = Math.max(1, max - min);
  const points = snapshots.map((item, index) => `${snapshots.length === 1 ? 50 : (index / (snapshots.length - 1)) * 100},${92 - ((item.final_price - min) / range) * 80}`).join(' ');
  const finalPrice = Math.max(0, product.price - product.coupon - product.platform_discount - product.member_discount - product.subsidy - product.pay_discount + product.shipping - product.gift_value);
  const review = detail?.review_evidence?.report;
  return <div className="detail-backdrop" onClick={onClose}><aside className="product-detail" onClick={(event) => event.stopPropagation()}><button className="detail-close" title="关闭" onClick={onClose}><X /></button><div className="detail-visual"><Laptop size={54} /><span>{product.platform || '来源待确认'}</span></div><div className="detail-title"><div><span>{product.brand} {product.model}</span><h2>{product.title}</h2></div><button className={favorite ? 'favorite active' : 'favorite'} onClick={() => onFavorite(product)}><Heart size={18} fill={favorite ? 'currentColor' : 'none'} />{favorite ? '已收藏' : '收藏'}</button></div><div className="detail-price"><div><span>预计到手</span><strong>{product.price ? money(finalPrice) : '待确认'}</strong></div><small>页面价 {money(product.price)} - 优惠 {money(product.coupon + product.platform_discount + product.member_discount + product.subsidy + product.pay_discount)} + 运费 {money(product.shipping)}</small></div><section><h3>规格与版本</h3><div className="spec-list"><span><b>型号</b>{product.model || '待确认'}</span><span><b>SKU</b>{product.sku || '待确认'}</span><span><b>成色</b>{product.condition || '待确认'}</span>{Object.entries(product.specs || {}).map(([key, value]) => <span key={key}><b>{key}</b>{value}</span>)}</div></section><section><h3>价格趋势</h3>{snapshots.length ? <><svg className="price-chart" viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label={`${snapshots.length} 条价格记录趋势`}><polyline points={points} vectorEffect="non-scaling-stroke" /></svg><div className="price-history-summary"><div><b>{money(history.lowest_price)}</b><span>历史最低</span></div><div><b>{money(history.average_price)}</b><span>平均到手价</span></div><div><b>{snapshots.length}</b><span>有效记录</span></div></div></> : <p className="detail-empty">还没有可追溯的历史价格。使用浏览器扩展采集后会形成趋势。</p>}</section><section><h3>评论证据</h3>{review ? <div className="review-evidence"><div><b>{review.risk_level === 'high' ? '高风险' : review.risk_level === 'medium' ? '中风险' : '低风险'}</b><span>{review.summary}</span><small>{review.sample_size} 条有来源评论 · 置信度 {Math.round(review.confidence * 100)}%</small></div>{review.issue_groups.slice(0, 3).map((group) => <details key={group.name}><summary>{group.name} · {Math.round(group.ratio * 100)}%</summary>{group.evidence.map((evidence, index) => <p key={index}>{evidence}</p>)}</details>)}</div> : <p className="detail-empty">尚未导入可追溯评论证据，因此不判断口碑风险。</p>}</section>{detail?.offers.length ? <section><h3>同款报价</h3><div className="detail-offers">{detail.offers.map((offer) => <article key={offer.url}><div><b>{offer.platform || '来源待确认'}</b><span>{offer.title}</span></div><strong>{offer.price ? money(offer.price) : '待确认'}</strong>{offer.url && <a href={offer.url} target="_blank" rel="noreferrer"><ExternalLink size={14} />来源</a>}</article>)}</div></section> : null}<section><h3>购买保障</h3><div className="assurance-row"><span><ShieldCheck />{product.official_store ? '官方/自营来源' : '店铺资质待核验'}</span><span><Clock3 />{product.return_days || 0} 天退货</span><span><PackageCheck />{product.warranty_months || 0} 个月保修</span></div></section>{detail?.alternatives.length ? <section><h3>可追溯替代项</h3><div className="detail-alternatives">{detail.alternatives.map((item) => <button key={item.url || item.sku} onClick={() => onCompare(item)}><span>{item.title}</span><b>{item.price ? money(item.price) : '价格待确认'}</b></button>)}</div></section> : null}{product.notes && <section><h3>来源说明</h3><p className="detail-empty">{product.notes}</p></section>}<div className="detail-actions">{product.url && <a href={product.url} target="_blank" rel="noreferrer"><ExternalLink size={16} />查看原商品</a>}<button onClick={() => onCompare(product)}>加入对比</button></div></aside></div>;
}

export function MobileNav({ view, onChange }: { view: string; onChange: (view: string) => void }) {
  const items = [['discover', '首页', Search], ['analyze', '对比', Sparkles], ['monitors', '省钱', Bell], ['messages', '消息', MessageSquare], ['account', '我的', UserRound]] as const;
  return <nav className="mobile-nav">{items.map(([key, label, Icon]) => <button key={key} className={view === key ? 'active' : ''} onClick={() => onChange(key)}><Icon size={19} /><span>{label}</span></button>)}</nav>;
}

export function SharedDecisionPage({ share }: { share: { title: string; share_type: string; payload: Record<string, unknown>; expires_at: string } | null | undefined }) {
  if (share === undefined) return <main className="shared-page"><div className="panel"><div className="consumer-empty" role="status" aria-live="polite"><i><Clock3 /></i><strong>正在读取分享</strong><span>正在校验链接和有效期，请稍候。</span></div></div></main>;
  if (!share) return <main className="shared-page"><div className="panel"><EmptyState icon={<Clock3 />} title="分享不存在或已失效" text="请向分享者获取新的 ValuSee 链接。" /></div></main>;
  const products = Array.isArray(share.payload.products) ? share.payload.products as ConsumerProduct[] : [];
  const result = share.payload.result as Record<string, unknown> | undefined;
  return <main className="shared-page"><header><a href="/">ValuSee</a><span>公开只读分享</span></header><section className="shared-hero"><span>{share.share_type === 'report' ? '购买决策报告' : '商品对比清单'}</span><h1>{share.title}</h1><p>此页面仅展示分享时的快照，不会随商品页面自动更新。有效期至 {day(share.expires_at)}。</p></section>{result && <section className="panel shared-summary"><h2>{String(result.summary || '决策摘要')}</h2><p>{String(result.recommendation_reason || '')}</p></section>}<section className="shared-products">{products.map((product, index) => <article className="panel" key={`${product.url}-${index}`}><div className="product-placeholder"><Laptop size={28} /></div><span>候选 {index + 1} · {product.platform || '来源待确认'}</span><h3>{product.title}</h3><b>{product.price ? money(product.price) : '价格待确认'}</b><small>{product.brand} {product.model}</small>{product.url && <a href={product.url} target="_blank" rel="noreferrer"><ExternalLink size={14} />查看来源</a>}</article>)}</section><footer>ValuSee 不保证分享快照中的价格仍然有效，下单前请回到原平台核验。</footer></main>;
}

function EmptyState({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <div className="consumer-empty"><i>{icon}</i><strong>{title}</strong><span>{text}</span></div>; }
